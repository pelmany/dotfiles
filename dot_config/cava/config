[general]
live-config = 1

mode = normal

framerate = 60
sensitivity = 100

[input]
method = pipewire
source = auto
; method = fifo
; source = /tmp/mpd.fifo

[output]
method = noncurses
orientation = bottom

[color]
theme = 'catppuccin-macchiato.cava'
